#include "main.h"

/**
 * print_most_numbers - Function to print most numbers
 * 0 throught to 9 except 2 and 4
 * followed by a newline
 * Return: 0
 */
void print_most_numbers(void)
{
	int h = '0';

	for (; h <= 9; h++)
{
	if (h != 2 && h != 4)
		_putchar(i);
}
_putchar('\n');
}
