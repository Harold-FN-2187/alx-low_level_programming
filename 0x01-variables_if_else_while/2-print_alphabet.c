#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */
int main(void)
{
char new;

	for (new = 'a'; new <= 'z'; new++)
		putchar(new);
		putchar('\n');
return (0);
}
