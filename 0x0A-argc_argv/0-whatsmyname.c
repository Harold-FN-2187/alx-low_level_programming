#include <stdio.h>
#include "main.h"
/**
 * main - prints its name, followed by a new line
 * @argc: count of command line arguments
 * @argv: array
 * Return: 0 - success
 */
int main()
{
	printf("%s\n", argv[0]);
	return (0);
}
